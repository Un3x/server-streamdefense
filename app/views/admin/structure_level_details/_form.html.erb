<%= form_with(model: [:admin, structure_level_detail]) do |form| %>
  <% if structure_level_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(structure_level_detail.errors.count, "error") %> prohibited this structure_level_detail from being saved:</h2>

      <ul>
        <% structure_level_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :structure_id %>
    <%= form.select :structure_id, Structure.all.pluck(:name, :id), { include_blank: "Select a structure" } %>
  </div>

  <div>
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div>
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
