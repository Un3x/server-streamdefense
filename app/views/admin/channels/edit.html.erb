<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing channel</h1>

  <%= render partial: "admin/channels/form", locals: { channel: @channel } %>

  <%= link_to "Show this channel", admin_channel_path(@channel), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to channels", admin_channels_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
