<%= form_with(model: channel) do |form| %>
  <% if channel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(channel.errors.count, "error") %> prohibited this channel from being saved:</h2>

      <ul>
        <% channel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :twitch_channel_id, style: "display: block" %>
    <%= form.text_field :twitch_channel_id %>
  </div>

  <div>
    <%= form.label :streamer_name, style: "display: block" %>
    <%= form.text_field :streamer_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
