<div class="grid">
  <h1>Editing season</h1>
  <div role="group">
    <%= button_to "Show this season", admin_season_path(@season), method: :get, class: %w[secondary] %>
    <%= button_to "Back to seasons", admin_seasons_path, method: :get, class: %w[contrast] %>
  </div>
</div>
<div class="container-fluid">
  <%= render partial: "admin/seasons/form", locals: { season: @season } %>
</div>
