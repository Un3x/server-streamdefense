<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing resource</h1>

  <%= render partial: "admin/resources/form", locals: { resource: @resource } %>

  <%= link_to "Show this resource",  admin_resource_path(@resource), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to resources", admin_resources_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
