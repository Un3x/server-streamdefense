<div class="grid">
  <h1>Editing resource</h1>
  <div role="group">
    <%= button_to "Show this resource", admin_resource_path(@resource), method: :get, class: %w[secondary] %>
    <%= button_to "Back to resources", admin_resources_path, method: :get, class: %w[contrast] %>
  </div>
</div>
<div class="container-fluid">
  <%= render partial: "admin/resources/form", locals: { resource: @resource } %>
</div>
