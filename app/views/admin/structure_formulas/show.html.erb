<div class="grid">
  <h1>
    <%= t('.title') %>
  </h1>
  <div role="group">
    <%= button_to "Edit this structure_formula", edit_admin_structure_formula_path(@structure_formula), method: :get, class: %w[secondary] %>
    <%= button_to "Destroy this structure_formula", admin_structure_formula_path(@structure_formula), method: :delete %>
    <%= button_to "Back to structure_formulas", admin_structure_formulas_path, method: :get, class: %w[contrast] %>
  </div>
</div>

<section class="container-fluid">
  <%= render partial: 'admin/structure_formulas/structure_formula', locals: { structure_formula: @structure_formula } %>
</section>
